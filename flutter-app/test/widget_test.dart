import 'package:flutter_test/flutter_test.dart';

// Placeholder test. It exists so that `flutter create` does not generate the
// default counter-app test (which refers to a class this app does not have
// and would show red errors in Android Studio).
void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
